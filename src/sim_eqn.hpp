#include "parser.hpp"
#include "errors.h"
#include <iostream>

//class Number;
//class ndArray;
/*
class sim_eqn
{
    private:
        //coefficient matrix,the rhs and the array for the solution
        Number **A,*B,*X;
        unsigned long n;
    public:
        //constructors
        sim_eqn();
        sim_eqn(unsigned long m);

        ndArray get();
        void set(ndArray _A,ndArray _B);
        void solve(unsigned long start);

        ~sim_eqn();
};
*/
